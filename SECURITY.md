# Security policy

Report vulnerabilities privately to the repository owner instead of opening a
public issue. Include affected versions, reproduction steps, and impact.

Never commit API credentials, cookies, private endpoints, personal data, or model
artifacts containing restricted data. Rotate any secret that reaches Git history.
The default client sends no authentication and only accesses configured public or
authorized endpoints.
