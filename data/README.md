# Data directory

- `raw/`: local source responses; ignored except `.gitkeep`.
- `processed/`: local canonical athlete tables; ignored except `.gitkeep`.
- `sample/`: committed synthetic data used for demonstrations.

Do not commit Volleyball World responses or athlete data unless the applicable
terms and licenses permit redistribution. The sample CSV is generated and does
not describe a real athlete.
